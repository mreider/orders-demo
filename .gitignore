node_modules/
package-lock.json
package.json
exports/
bin/

scripts/capture.js

.DS_Store

# PowerPoint opens a ~$file.pptx lock file in the same dir; never commit it.
~$*.pptx

# The built PPTX under presentation/ is checked in (see scripts/build-slides.sh).
!presentation/**/*.pptx
